apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: crossplane
  labels:
    prometheus: app-monitoring
spec:
  namespaceSelector:
    any: true
  selector:
    matchLabels:
      app: crossplane
  endpoints:
  - port: web
