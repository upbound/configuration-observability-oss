apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: providers
  namespace: operators
  labels:
    release: oss
spec:
  namespaceSelector:
    any: true
  selector:
    matchLabels:
      app: providers
  endpoints:
  - port: http-metrics
