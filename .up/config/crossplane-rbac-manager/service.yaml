kind: Service
apiVersion: v1
metadata:
  name: crossplane-rbac-manager
  namespace: upbound-system
  labels:
    app: crossplane-rbac-manager
spec:
  selector:
    app: crossplane-rbac-manager
  ports:
  - name: http-metrics
    port: 8080
