apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: crossplane
  namespace: operators
  labels:
    release: oss
spec:
  namespaceSelector:
    any: true
  selector:
    matchLabels:
      app: crossplane
  endpoints:
  - port: http-metrics
